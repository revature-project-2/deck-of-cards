import {LoadCardDataPipe} from './load-card-data.pipe';

describe('LoadCardDataPipe', () => {
  it('create an instance', () => {
    const pipe = new LoadCardDataPipe();
    expect(pipe).toBeTruthy();
  });
});
